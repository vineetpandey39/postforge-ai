export const metadata = {
  title: 'PostForge AI — @aibyvineet',
  description: 'Verified AI content generator for @aibyvineet'
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
